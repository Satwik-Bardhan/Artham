<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active Selected State -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/chk_primary_blue" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Inactive State: Clean surface background with subtle divider outline -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/chk_surfaceColor" />
            <stroke android:width="1dp" android:color="?attr/chk_dividerColor" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>